@{
	Layout = "_Layout";
}

@using Bakery.Models;

<h1>Flavors</h1>

@if (Model.Count == 0)
{
	<h4>0 flavors available</h4>
}
else
{
	<ul>
		@foreach (Flavor flavor in Model)
		{
			<li>@Html.ActionLink($"{flavor.Name}", "Details", new {id=flavor.FlavorId})</li>
		}
	</ul>
}

<p>@Html.ActionLink("Add a new flavor", "Create")</p>